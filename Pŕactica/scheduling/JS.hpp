#include <iostream>
using namespace std;

void fill(int*, int);
void sort(int*, int, int(int, int));
void qs(int*, int, int, int(int,int,int*), int*);
void qs_debug(int*, int, int);
void JS(int*, int*, int);
void print(int*, int, int);

struct solution{
  int n;
  int* SC_idx;
};
